language: java
matrix:
  include:
    - os: osx
    - os: linux
      jdk: oraclejdk8
    - os: linux
      jdk: oraclejdk7

script: "mvn cobertura:cobertura"

after_success:
  - bash <(curl -s https://codecov.io/bash)
